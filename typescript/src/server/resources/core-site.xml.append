<property>
  <name>hadoop.security.authentication</name>
  <value>${env.hive.secureMode ? 'kerberos' : 'simple'}</value>
</property>

<property>
  <name>ipc.client.fallback-to-simple-auth-allowed</name>
  <value>${env.hive.secureMode}</value> 
</property>

</configuration>
